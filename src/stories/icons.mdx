import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks'
import * as Icons from '@storybook/blocks'

<Meta title="System/Icons" />

<IconGallery>
  {Object.entries(Icons)
    .filter(([name]) => !name.startsWith('Icon'))
    .map(([name, Icon]) => (
      <IconItem>
        <Icon />
        <span>{name}</span>
      </IconItem>
    ))}
</IconGallery>
